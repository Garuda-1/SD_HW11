server:
  port: 8001

spring:
  datasource:
    url: jdbc:h2:mem:testData;mode:PostgreSQL
    username: sa
    password: sa
  liquibase:
    change-log: classpath:/db/changelog/main-changelog.yaml
    enabled: true